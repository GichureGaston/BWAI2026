services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8080"
    env_file:
      - .env
    environment:
      - PORT=8080

  frontend:
    build:
      context: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
